{
  "name": "medicheck-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "메디체크 코리아 화면. esbuild 로 한 덩어리로 묶어 Spring 정적 폴더에 넣는다 — Vite 를 쓰지 않는 이유는 index.html 을 하나 더 만들기 때문이다.",
  "scripts": {
    "build": "node build-frontend.mjs",
    "watch": "node build-frontend.mjs --watch",
    "verify": "node verify-screens.mjs"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "playwright": "1.62.1"
  }
}
